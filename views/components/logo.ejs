<% if(authorName){%>
    <a href="/personal/info<%if(!!authorId){%>?author=<%=authorId%><%}%>">
    <h1 style="font-size:inherit">
    <%= authorName %>
    </h1>
    </a>
<% }else {%>
    <!-- Beau's&nbsp;&nbsp;BLOG -->
    <a href="/">
        <h1 style="font-size:inherit">
        <% if(!user || showDefaultLogo){%>
             幽忧志
        <% }else{ %>
            <%=user.nickname%>
        
        <% } %>
        </h1>
    </a>
<% } %>