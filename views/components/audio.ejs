<audio controls="controls" class="m-audio" preload="auto">
    Your browser does not support the <code>audio</code> element.  
    <% audios.forEach(function(item){  
    %>
        <source src="<%=item%>" >
    <%});%>
</audio>